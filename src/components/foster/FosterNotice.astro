---
import Checkbox from '../lib/Checkbox.astro';

const notice = {
    name: "foster-notice",
    legend: "Check the box below",
    divClass: "col-6",
    required: "true",
    spanClass: "validity",
    entries: {
        "foster-notice": {
          value: "notice acknowledged",
          text: "I acknowledge that I have read the above paragraph and understand the terms of Fostering."
        },
    },
};
const onchange = `validateCheckbox('${notice.name}')`;
---
<div class="col-12">
  <h2>Notice</h2>
  <p>
    When you foster, you must keep your foster kitty indoors, safe, healthy and socialized.  
    For fostering, please be prepared to continue fostering your rescue is ready for adoption and eligible to move to the Feline Good Social Club adoption center.  </p>
  <fieldset required={notice.required}>
    <legend>{notice.legend}<span class={notice.spanClass}></span></legend>
    <div class = "one-col-full-width">
      {Object.keys(notice.entries).map((key) =>
        <Checkbox
          divClass = {notice.divClass}
          checkboxName = {notice.name}
          checkboxId = {key}
          checkboxValue = {notice.entries[key].value}
          checkboxRequired = {notice.required}
          checkboxOnchange = {onchange}
          labelText = {notice.entries[key].text}
        />
      )}
    </div>
  </fieldset>
</div>
